// Package constants defines global application constants for operator.
package constants

const (
	// AppName is the binary and application name.
	AppName = "operator"
	// AliasName is the shorthand alias for operator.
	AliasName = "opr"
	// Version is the current semantic release version of operator.
	Version = "0.1.0"
)
